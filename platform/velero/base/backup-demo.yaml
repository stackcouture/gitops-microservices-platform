apiVersion: velero.io/v1
kind: Backup
metadata:
  name: vote-backup-demo
  namespace: velero
spec:
  includedNamespaces:
    - vote

  ttl: 720h0m0s

  storageLocation: default

  snapshotVolumes: false