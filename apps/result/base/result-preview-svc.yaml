apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
    environment: dev
  name: result-preview-svc 
  namespace: vote
spec:
  sessionAffinity: ClientIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    app: result
