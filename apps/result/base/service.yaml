apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: result-argo-svc
  namespace: argo-demo
spec:
  sessionAffinity: ClientIP
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    app: result
