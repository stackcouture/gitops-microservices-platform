apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: result-stable-svc
  namespace: vote
spec:
  sessionAffinity: ClientIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    app: result
