apiVersion: v1
kind: ResourceQuota
metadata:
  name: redis-quota
  namespace: redis
spec:
  hard:
    requests.cpu: "1"
    limits.cpu: "2"

    requests.memory: 4Gi
    limits.memory: 8Gi

    pods: "20"
    persistentvolumeclaims: "10"