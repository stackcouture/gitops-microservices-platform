apiVersion: v1
kind: ResourceQuota
metadata:
  name: argocd-quota
  namespace: argocd
spec:
  hard:
    requests.cpu: "500m"
    limits.cpu: "1"

    requests.memory: 1Gi
    limits.memory: 2Gi

    pods: "20"